import React from 'react';
import App from '../components/App/Index';

const NotFoundPage = () => (
  <App>
    Nothing is found here, please refresh or navigate back to Home.
  </App>
);

export default NotFoundPage;
